package enums

enum class SpotifyMenu(val id: String, val desc: String) {
    LOGIN(id="1", desc = "Gunakan username dan password untuk masik aplikasi"),
    PROFILE(id="2", desc = "User informasi yang login")
}